name: D1 Tower ten-cell material constants

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/d1-tower-ten-cell-material-constants.yml'
      - 'tools/d1_world_material_constant_export.py'

permissions:
  contents: read
  actions: read

jobs:
  constants:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.13'}
      - name: Recover exact manifest, package corpus and runtime
        env: {GH_TOKEN: '${{ github.token }}'}
        run: |
          set -euo pipefail
          mkdir -p runtime recovered prior reports
          gh run download 33987189885 -n d1-tower-ten-cell-corrected-textured-reports -D prior
          test -f prior/reports/material_texture_manifest_ten_cell.json
          curl --fail --location --retry 3 'https://raw.githubusercontent.com/Crauzer/OodleSharp/master/OodleSharp/oo2core_3_win64.dll' -o runtime/oo2core_3_win64.dll
          echo '682c0aad216fae443e0f9561876cfabfddaeffcd48e5990613ad2cf47c49fa62  runtime/oo2core_3_win64.dll' | sha256sum --check --strict
          curl --fail --location --retry 3 'https://raw.githubusercontent.com/v4nguard/tiger-pkg/657f41c0851001b2d371592b2f7a5cb9c686ddb4/liblinoodle3.so' -o runtime/liblinoodle3.so
          echo '0167cfd2b31623ce15afd129c3ad568776d63af223824ab085254b7ea0730e35  runtime/liblinoodle3.so' | sha256sum --check --strict
          curl --fail --location --retry 3 'https://crypt.cohae.dev/destiny/ps4/packages/latest/packages.txt' -o reports/packages.txt
          python tools/d1_tower_recover_current_corpus.py --package-list reports/packages.txt --out-dir recovered --report reports/current_corpus.json
      - name: Export exact material vec4 constant containers
        env: {LINOODLE_SKIP_DLLMAIN: '1'}
        run: |
          set -euo pipefail
          mapfile -t files < <(find recovered -maxdepth 1 -name '*.pkg' -type f | sort)
          args=(); for f in "${files[@]}"; do args+=(--snapshot "$f"); done
          python tools/d1_world_material_constant_export.py "${args[@]}" --runtime runtime \
            --manifest prior/reports/material_texture_manifest_ten_cell.json \
            --out reports/ten_cell_material_constants.json
          python - <<'PY'
          import json
          d=json.load(open('reports/ten_cell_material_constants.json'))
          out={}
          for mh in ['80C988A8','80C988A9']:
              c=d['material_constants'][mh]['ps']
              out[mh]={'container':c,'record':d['containers'].get(c)}
          open('reports/highlighted_material_constants.json','w').write(json.dumps(out,indent=2)+'\n')
          print(json.dumps(out,indent=2))
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: d1-tower-ten-cell-material-constants
          path: reports/
          retention-days: 14
          compression-level: 6
