name: D1 Crota raid scripted StringHash preimages

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/d1-crota-raid-scripted-stringhash-preimages.yml'
      - 'tools/d1_remote_stringhash_preimage_scan.py'

permissions:
  contents: read
  actions: read

env:
  UNIVERSAL_CATALOG_ARTIFACT_ID: '9994811238'
  UNIVERSAL_CATALOG_ZIP_SHA256: '404a6b4cdc34c7d305eb7b120df3b6f7d3f1121600631c2b3dbc66efd29d144a'
  UNIVERSAL_CATALOG_JSON_SHA256: '58255411fd53a8c64eb6f55e0da76a103d494ea3606096b2d995a5b5f35eca72'

jobs:
  scan:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.13'}
      - name: Recover exact universal catalog
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p input/catalog runtime out
          curl -fsSL --retry 4 -H "Authorization: Bearer $GH_TOKEN" -H 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/artifacts/${UNIVERSAL_CATALOG_ARTIFACT_ID}/zip" -o input/catalog.zip
          echo "${UNIVERSAL_CATALOG_ZIP_SHA256}  input/catalog.zip" | sha256sum --check --strict
          unzip -q input/catalog.zip -d input/catalog
          echo "${UNIVERSAL_CATALOG_JSON_SHA256}  input/catalog/D1_UNIVERSAL_MEMBER_CATALOG.json" | sha256sum --check --strict
      - name: Install verified Oodle runtime
        run: |
          set -euo pipefail
          curl --fail --location --retry 3 'https://raw.githubusercontent.com/Crauzer/OodleSharp/master/OodleSharp/oo2core_3_win64.dll' -o runtime/oo2core_3_win64.dll
          echo '682c0aad216fae443e0f9561876cfabfddaeffcd48e5990613ad2cf47c49fa62  runtime/oo2core_3_win64.dll' | sha256sum --check --strict
          curl --fail --location --retry 3 'https://raw.githubusercontent.com/v4nguard/tiger-pkg/657f41c0851001b2d371592b2f7a5cb9c686ddb4/liblinoodle3.so' -o runtime/liblinoodle3.so
          echo '0167cfd2b31623ce15afd129c3ad568776d63af223824ab085254b7ea0730e35  runtime/liblinoodle3.so' | sha256sum --check --strict
      - name: Search raid package text for exact FNV1 preimages
        run: |
          set -euo pipefail
          export LINOODLE_SKIP_DLLMAIN=1
          python -m py_compile tools/d1_remote_stringhash_preimage_scan.py
          python tools/d1_remote_stringhash_preimage_scan.py \
            --package-id 0x0447 --package-id 0x044B --package-id 0x0450 --package-id 0x0458 \
            --target 0x7BF2DD3B --target 0x64C53DB9 --target 0x23CFE642 \
            --target 0x4E304AA7 --target 0xDA42832F --target 0xA8B2171D \
            --target 0x609BA2DF --target 0xABD81055 --target 0x80674684 \
            --target 0xA2DA2AC9 --target 0x8FC74ADA --target 0x72C51822 \
            --target 0x19C57E65 --target 0x19C57E66 --target 0x19C57E67 \
            --target 0x19C57E60 --target 0x003EB432 --target 0xE816F049 \
            --target 0x5CFE4C22 --target 0x19C57E61 --target 0x19C57E62 \
            --target 0x4527C498 \
            --member-catalog input/catalog/D1_UNIVERSAL_MEMBER_CATALOG.json \
            --base-url 'https://crypt.cohae.dev/destiny/ps4/packages/latest' --part-count 10 \
            --runtime runtime --max-entry-size 8000000 \
            -o out/CROTA_RAID_SCRIPTED_STRINGHASH_PREIMAGES.json | tee out/stdout.txt
      - name: Summarize exact preimages
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          d=json.load(open('out/CROTA_RAID_SCRIPTED_STRINGHASH_PREIMAGES.json'))
          print('HIT_COUNTS',d['hit_counts'])
          for x in d['hits']:
              print('PREIMAGE',x['target'],repr(x['preimage']),'SOURCE',x['source_tag_hash'],'CLASS',x['source_reference'],'OFFSET',hex(x['byte_offset']))
          PY
          sha256sum out/* > out/SHA256SUMS.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: D1-CROTA-RAID-SCRIPTED-STRINGHASH-PREIMAGES
          path: out/
          retention-days: 30
          if-no-files-found: error
