name: D1 Tower actor location to visual signature join v2

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/d1-tower-actor-location-visual-signature-join-v2.yml'
      - 'tools/d1_tower_actor_location_visual_signature_join.py'
      - 'tools/d1_tower_actor_location_visual_signature_join_v2.py'

permissions: {contents: read, actions: read}

env:
  LOCATION_ARTIFACT_ID: '10035502868'
  LOCATION_ARTIFACT_SHA256: '6306fa83be0334fe2744776b3b88921da2c90e21831cf095dd8a09c17f2ac53c'
  SIGNATURE_ARTIFACT_ID: '10035991740'
  SIGNATURE_ARTIFACT_SHA256: '4bb937be6206900ff241c93a40dd54e4e7b00387c30dfd28f991039fd5760c6f'
  RUNTIME_ARTIFACT_ID: '10000412397'
  RUNTIME_ARTIFACT_SHA256: 'fa8e5dd2b82bc5527221baae6080289af9aa598451d6a5594d8b5e0db22bc4d6'

jobs:
  join:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      GH_TOKEN: '${{ github.token }}'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.13'}
      - name: Recover exact source checkpoints
        run: |
          set -euo pipefail
          mkdir -p input/location input/signatures input/runtime out
          pull() {
            id="$1"; sha="$2"; zip="$3"; dst="$4"
            curl -fsSL --retry 4 -H "Authorization: Bearer $GH_TOKEN" -H 'X-GitHub-Api-Version: 2022-11-28' \
              "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/artifacts/${id}/zip" -o "$zip"
            echo "${sha}  ${zip}" | sha256sum --check --strict
            unzip -q "$zip" -d "$dst"
          }
          pull "$LOCATION_ARTIFACT_ID" "$LOCATION_ARTIFACT_SHA256" input/location.zip input/location
          pull "$SIGNATURE_ARTIFACT_ID" "$SIGNATURE_ARTIFACT_SHA256" input/signatures.zip input/signatures
          pull "$RUNTIME_ARTIFACT_ID" "$RUNTIME_ARTIFACT_SHA256" input/runtime.zip input/runtime

      - name: Close scoped EntitySK model to D912 visual join
        run: |
          set -euo pipefail
          python -m py_compile tools/d1_tower_actor_location_visual_signature_join.py tools/d1_tower_actor_location_visual_signature_join_v2.py
          python tools/d1_tower_actor_location_visual_signature_join_v2.py \
            --locations input/location/D1_TOWER_EIGHT_SCENARIO_SPAWNED_LOCATION_MANIFEST.json \
            --signatures input/signatures/D1_TOWER_ACTOR_VISIBLE_MATERIAL_SIGNATURES_FINAL.json \
            --runtime-closure input/runtime/TOWER_SPAWNED_ACTOR_RUNTIME_CLOSURE.json \
            -o out/D1_TOWER_ACTOR_LOCATION_VISUAL_SIGNATURE_JOIN_V2.json | tee out/stdout.txt
          python - <<'PY'
          import json
          d=json.load(open('out/D1_TOWER_ACTOR_LOCATION_VISUAL_SIGNATURE_JOIN_V2.json'))
          assert d['status']=='D1_TOWER_ACTOR_LOCATION_VISUAL_SIGNATURE_JOIN_COMPLETE',d['violations']
          assert d['scenario_count']==8 and d['exact_location_alternative_count']==547,d
          assert d['resolved_visual_location_alternative_count']==547 and d['resolved_visual_model_count']==13,d
          assert d['resolved_visual_variant_count']==29,d
          assert d['resolution_method_counts']=={'exact_d912_owner_configuration_signature':464,'single_signature_model':83},d
          p=d['runtime_model_edge_projection']
          assert p['source_actor_count']==57 and p['source_exact_model_edge_count']==57 and p['source_unique_model_count']==13,p
          assert p['all_source_runtime_violations_are_superseded_clip_dimension_checks'] is True,p
          assert p['native_retarget_semantics_promoted_by_this_join'] is False,p
          assert all(v is False for v in d['gates'].values()),d['gates']
          print('LOCATION_VISUAL_JOIN_V2_GREEN',d['resolved_visual_location_alternative_count'],'rows',d['resolved_visual_variant_count'],'used variants')
          PY
          find out -type f -print0 | sort -z | xargs -0 sha256sum > out/SHA256SUMS.txt

      - uses: actions/upload-artifact@v4
        with:
          name: D1-TOWER-ACTOR-LOCATION-VISUAL-SIGNATURE-JOIN-V2
          path: out/
          retention-days: 90
          if-no-files-found: error
          compression-level: 6
